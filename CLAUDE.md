# Code Royale Bot — контекст проекта (handoff для Claude Code)

Этот файл — полный контекст проекта. Положи его в корень репозитория (можно переименовать в `CLAUDE.md`, чтобы Claude Code читал его автоматически).

## Цель

Самый успешный бот для CodinGame **Code Royale** (раздел Bot Programming): пройти лиги Wood → Bronze → Silver → Gold → Legend, затем гонка за топ лидерборда.

## Формат работы (важно)

- Пользователь (Andrey) руководит, Claude делает и **перед каждым этапом расписывает, что и зачем будет сделано, понятным языком** (он C#-разработчик, но экосистему CodinGame не знает).
- В этом проекте подробные объяснения приветствуются — это осознанное исключение из его обычного «пиши компактно».
- Обращение на «ты».
- Двигаемся поэтапно: разложили этап → «делай» → сделали → следующий.

## Принятые решения

1. **Язык: C#**, порт симулятора+поиска в C++ — только если реально упрёмся в CPU на этапе гонки за топ.
2. **Пропускаем этап «умной эвристики»**: расходный примитивный бот (~50 строк) только чтобы выйти из Wood в Bronze и служить спарринг-партнёром; вся реальная работа сразу в симулятор + поиск.
3. Архитектура целевого бота (по образцу победителей контеста):
   - точный симулятор движка (порт открытого рефери 1-в-1, бит-в-бит, без аллокаций);
   - beam search для действий королевы (глубина 10–15, ширина 25–50, 8 направлений + BUILD-действия; противник моделируется почти статичным);
   - eval: своё HP (большой вес) − HP врага/10; башни через (недостающее HP)²; доход шахт; штраф за <1 или >3 казарм; близость казарм к врагу; случайный тай-брейк;
   - макро (что тренировать) — отдельной простой эвристикой, не поиском (так делал и победитель).
4. Все изменения принимаются **только по статистике локальной арены** (сотни игр), не «на глаз».

## Правила игры (шпаргалка, сверено с исходниками рефери)

- Карта 1920×1000, **250 ходов на арене** (в исходниках рефери на GitHub — 200; в реплеях арены 501 кадр = 250 ходов), лимит **50 мс/ход** (первый ход ~1000 мс). Победа: убить королеву врага или больше HP на последнем ходу.
- Королева: 100 HP, скорость 60/ход, радиус 30. Касается сайта → строит. Касанием сносит вражеские шахты и казармы (башни — нет).
- Ввод/вывод: каждый ход читаем состояние из stdin, печатаем 2 строки: `MOVE x y | BUILD id TYPE | WAIT` и `TRAIN [id...]`.
- Сайты (12–24 на карте, симметрично). Постройки:
  - **Шахта** (MINE): доход +incomeRate/ход, повторный BUILD апгрейдит до maxMineSize сайта; золото в сайте конечное, кончилось → шахта исчезает.
  - **Башня** (TOWER): 200 HP при постройке, повторный BUILD +100, максимум 800; радиус растёт с HP (площадь покрытия = HP × 1000 + площадь сайта); тает −4 HP/ход; бьёт ближайшего вражеского крипа в радиусе, иначе королеву; урон больше вблизи (мин. 1 по королеве, мин. 3 по крипам, +1 за каждые 200 ед. недолёта до края радиуса).
  - **Казарма** (BARRACKS-KNIGHT/ARCHER/GIANT).
- Юниты (все теряют 1 HP/ход и умирают сами):
  - **Рыцари**: 80 g → 4 шт., скорость 100, HP 30, бегут к вражеской королеве, 1 урона за касание; тренировка 5 ходов.
  - **Лучники**: 100 g → 2 шт., скорость 75, HP 45, дальность 200, урон 2 (10 по гигантам); бьют только крипов, королеву не трогают (чистая оборона); 8 ходов.
  - **Гиганты**: 140 g → 1 шт., скорость 50, HP 200, идут к ближайшей вражеской башне, 80 урона за удар; 10 ходов.
- Порядок хода в движке: действия игроков → движение/бой крипов (коллизии в 5 подшагов!) → действия построек → доход → смерть крипов → проверка конца. Порядок и округления критичны для точности симулятора.
- **Wood-лиги играют по урезанным правилам** (фиксированный доход +10, часть построек отключена); полные правила — с Bronze. До Bronze не переусложнять.

## Что выигрывает (выжимка постмортемов топов контеста)

- Весь топ = точный симулятор + поиск для королевы: RoboStac (1-е) — beam search до глубины 15, ~8000 симуляций/ход; Agade (3-е) — simulated annealing глубиной 7; IloIlo (4-е) — генетика 10–14. Чистые эвристики в топ не попали.
- Королева — 90% игры: кайтинг от рыцарей, заходы мимо башен. Это даёт только поиск.
- Мета: рыцарский раш на стартовые 100 g (открытие Agade: 2 шахты + казарма + волна) + экономика на шахтах + оборона башнями; **гиганты — ответ на башенные крепости**; лучники почти бесполезны.
- Противника моделировать почти не нужно («вражеская королева мало что может сделать» — RoboStac).
- Главный урок всех топов: **точность симулятора** (тест: на реальных реплеях предсказывать следующее состояние) и локальное тестирование статистикой.

## План этапов и статус

- [ ] **Этап 1 — инфраструктура**: C#-проект + скрипт склейки в один файл (CG принимает только один файл, вставка в браузере вручную); расходный Wood-бот; сборка локального рефери (Kotlin/JVM, репо csj/code-royale); арена self-play (cg-brutaltester и/или psyleague); выход в Bronze. Оценка 1–2 сессии.
- [ ] **Этап 2 — симулятор**: порт рефери 1-в-1 на C#, тесты точности на реплеях (совпадение состояний бит-в-бит).
- [ ] **Этап 3 — beam search + базовый eval** → Silver/Gold.
- [ ] **Этап 4 — тюнинг** eval и макро через self-play и CGBenchmark → Legend.
- [ ] **Этап 5 — гонка за топ**: разбор реплеев конкретных противников, подстройка; при нехватке CPU — порт в C++.

**Статус на сейчас: код не написан, ни одна задача этапа 1 не начата.** Следующее действие — этап 1.

## Технические заметки

- Проверить актуальную версию .NET на серверах CG и зафиксировать её локально.
- Сабмит на CG — всегда вручную (API нет): отдаём пользователю склеенный файл.
- CGBenchmark (замеры winrate против конкретных ботов на серверах CG) требует логин/пароль CG → запускать на машине пользователя; подготовить конфиг и инструкцию.
- Симулятор писать на структурах, без GC-аллокаций в горячем цикле; квадраты расстояний; аварийный ход при подходе к 50 мс.
- Аккаунт CodinGame у пользователя: уточнить/завести при первом сабмите.

## Источники

- Рефери (исходники правил): https://github.com/csj/code-royale
- Постмортем RoboStac (1-е место): https://github.com/robostac/cg-code-royale-postmortem
- Постмортем Agade (3-е место): https://github.com/Agade09/Agade-Code-Royale-Postmortem/blob/master/Agade_CR_Postmortem.md
- Форум Feedback & Strategies: https://forum.codingame.com/t/code-royale-cc03-feedback-strategies/30903
- Страница игры: https://www.codingame.com/multiplayer/bot-programming/code-royale

---

# Дополнение Claude Code (сессия 1, 2026-09-08): сверка с кодом рефери

Ниже — факты из исходников рефери `csj/code-royale` (Kotlin, последний коммит март 2019, т.е. мультиплеерная версия после контеста). Клонируется из песочницы: `git clone --depth 1 https://github.com/csj/code-royale` (файлы `src/main/kotlin/com/codingame/game/{Referee,Structures,Characters,MapBuilding,Constants,Vector2}.kt`, ~1500 строк). При расхождении со шпаргалкой выше верить этому разделу, при сомнениях — коду.

## Лиги (`Referee.init`, по `gameManager.leagueLevel`)

| Лига | Уровень | Шахты | Башни | Гиганты | Доход | Сайтов | HP королевы |
|---|---|---|---|---|---|---|---|
| Wood 3 | 1 | нет | нет | нет | фикс. +10/ход | 2×(6..12) | 100 |
| Wood 2 | 2 | нет | да | да | фикс. +10/ход | 2×(6..12) | 100 |
| Wood 1 | 3 | да | да | да | только шахты | 24 | 100 |
| Bronze+ | ≥4 | да | да | да | только шахты | 24 | **случайно 25..100 с шагом 5, одинаковое у обоих** |

- **BUILD типа, отключённого в лиге** (TOWER/MINE/BARRACKS-GIANT в Wood 3, MINE в Wood 2) = `PlayerInputException` = **бот убит**. Лига из ввода не определяется → константа `DefaultLeague` = 4 (Bronze и выше; Wood пройден, склейка только под неё).
- HP королевы читать из ввода на первом ходу (health своей королевы). Постмортемы контеста писались при 100 HP — дебюты оттуда проверять по реплеям арены.
- Золото на карте конечно: 24 сайта × 200..350. Сайты парами, центрально-симметричные, радиус 60..90, золото 200..250, размер шахты 1..3; ближе 500 к центру +50 золота и +1 к размеру, ближе 200 — ещё +50/+1 (до 5).

## Ввод/вывод

- Старт: `numSites`, затем `siteId x y radius`. Ход: `gold touchedSite`; по каждому сайту `siteId gold maxMineSize structureType owner param1 param2`; `numUnits`; `x y owner unitType health`.
- `unitType`: −1 королева, 0 рыцарь, 1 лучник, 2 гигант. `structureType`: −1 пусто, 0 шахта, 1 башня, 2 казарма. `owner`: −1 ничей, 0 мой, 1 чужой.
- Шахта: `param1` = доход (−1 если не видно), `param2` = −1. Башня: `param1` = HP, `param2` = радиус атаки. Казарма: `param1` = ходов до конца тренировки (0 — простаивает), `param2` = тип крипа.
- **Видимость**: `gold` и `maxMineSize` сайта показываются, только если постройка моя или сайт ближе 300 (`QUEEN_VISION`) к моей королеве, иначе −1. Доход чужой шахты тоже −1 без видимости. **Золото противника не даётся** — вести счётчик: 100 на старте + видимые доходы − замеченные тренировки.
- Порядок юнитов во вводе: крипы игрока 0 в порядке спавна, его королева, крипы игрока 1, его королева. Этот порядок нужен симулятору (см. коллизии).
- Вывод: строка 1 `WAIT` | `MOVE x y` | `BUILD id TYPE` (TYPE: `MINE`, `TOWER`, `BARRACKS-KNIGHT`, `BARRACKS-ARCHER`, `BARRACKS-GIANT`); строка 2 `TRAIN [id ...]`. Лишний токен, двойной пробел, незнакомая команда, несуществующий id — **бот убит**.
- `TRAIN`: повтор id, чужая/не казарма, тренирующаяся казарма, сумма стоимости > золота — warning, **весь TRAIN отклонён целиком**, золото не списывается.
- `BUILD id TYPE`: если королева касается сайта (dist < 30 + r + 5) — постройка планируется; **иначе это MOVE к центру сайта** (удобно как «иди и строй», постройка случится на следующем ходу после подхода).
- Строить на чужой постройке **нельзя** (warning). Касание королевой сносит чужую шахту/казарму (не башню) на фазе крипов того же хода → строить можно со следующего хода. Перестройка своей тренирующейся казармы — warning. Своя простаивающая постройка заменяется чем угодно; MINE на своей шахте = +1 доход до `maxMineSize`, TOWER на своей башне = +100 HP до 800.
- Обе королевы строят на одном сайте в один ход → выполняется одна, по чётности хода (`removeAt(turn % 2)`).
- `touchedSite` — `singleOrNull`: при касании двух сайтов сразу −1, но BUILD проверяется по дистанции и работает.
- Время: 1000 мс первый ход, 50 мс остальные. **250 ходов на арене** (`Consts.MaxTurns`; локальный рефери — `-Dturn.max`, в `match.sh` переменная `TURN_MAX`, по умолчанию 250); конец — смерть королевы или сравнение HP, **при равенстве ничья**; убитый за ошибку бот получает −1.

## Порядок хода (`Referee.gameTurn`) — симулятор копировать построчно

1. `goldPerTurn = 0`, отправка состояния, чтение двух строк.
2. Действия игроков (сначала игрок 0, потом 1): **сначала TRAIN** (списание, `progress = 0`, `isTraining`), **потом команда королевы** — MOVE/BUILD-подход сразу двигает королеву на ≤ 60 без проверки коллизий; постройки откладываются и выполняются после обоих игроков.
3. Крипы: общий список, **стабильно отсортирован по типу** (KNIGHT, ARCHER, GIANT; внутри типа — порядок ввода). `repeat(5)`: каждый крип `move(1/5)`, затем **одна** итерация `collisionCheck` по всем сущностям. Затем `dealDamage` всех крипов; **любой чужой крип** (и лучник, и гигант), касающийся своего ближайшего сайта, сносит на нём чужую шахту; всем крипам −1 HP; королевы сносят чужие шахты/казармы на своём ближайшем сайте в касании.
4. Постройки `act()` в порядке сайтов: **шахта** — доход `min(rate, gold)` в тот же ход, при `gold ≤ 0` исчезает; **башня** — цель: ближайший чужой крип, если dist до центра < `attackRadius`, иначе чужая королева, если в радиусе; потом `health −= 4`, `attackRadius = (int)sqrt((health·1000 + π·r²)/π)`, при `health ≤ 0` исчезает; **казарма** — `progress++`, при `progress == buildTime` спавн (см. ниже).
5. Фиксированный доход (только Wood).
6. Удаление крипов с `health == 0` — **после башен**: крип, умерший от «−1 в ход», в этот ход ещё цель для башни.
7. Проверка смерти королев, конец игры.
8. **Все координаты округляются до целых** (`snapToIntegers`, round half up = `Math.round`). Поэтому ход начинается ровно из состояния во вводе и бит-в-бит достижим; внутри хода — double. Исключение — первый ход: стартовые позиции королев (200,200) и (1720,800) после расталкивания сайтами не округлены, во вводе округлены.

## Формулы (`Characters.kt`, `Structures.kt`, `MapBuilding.kt`)

- `towards(target, d)`: если `dist < d` → `target`, иначе `loc + (target − loc).normalized · d`, где `normalized = (x/len, y/len)`, `len = sqrt(x²+y²)`; при `len < 1e-6` → `(1, 0)`. Сравнения дистанций — по квадратам.
- Свежепостроенная **башня**: `attackRadius = 0` → в ход постройки не стреляет, к следующему ходу HP 196. Урон: `shot = dist(центр) − r_сайта`, `damage = min + (int)((attackRadius − shot) / 200)`, min 3 крипу, 1 королеве.
- **Рыцарь** (r 20, масса 400, HP 30, скорость 100): если dist до чужой королевы > 20 + 30 → шаг к точке `королева + (я − королева).resizedTo(3)`; урон 1, если dist < 20 + 30 + 5.
- **Лучник** (r 25, масса 900, HP 45, скорость 75, дальность 200): цель — ближайший чужой крип, без цели идёт к **своей** королеве; стреляет, если dist < 25 + r_цели + 200 + 5; урон 2, гиганту 10. Королеву не бьёт.
- **Гигант** (r 40, масса 2000, HP 200, скорость 50): идёт к ближайшей чужой башне, **без башен стоит на месте**; бьёт **первую по порядку сайтов** (не ближайшую) чужую башню в касании (dist < 40 + r + 5), −80 HP.
- **Спавн**: позиция казармы + `(±i, ±i)` для i-го крипа (знак: игрок 0 плюс, игрок 1 минус), затем `towards(чужая королева, 30)`, затем полное расталкивание (до 999 итераций). Спавн в фазе построек → в ход спавна крипы не ходят.
- **Коллизии** (`collisionCheck`): список сущностей = крипы игрока 0, крипы игрока 1, королева 0, королева 1, сайты по id. Для каждого `u1` по порядку: сначала кламп в карту 1920×1000 (сайты с отступом 90 + r, юниты — r), затем для каждого `u2 ≠ u1` по порядку с **уже сдвинутыми** координатами: `overlap = r1 + r2 − dist`; если `> 1e-6`: доли сдвига `d1 = m2/(m1+m2)`, `d2 = m1/(m1+m2)`; сайт (масса 0) не двигается, второй берёт весь overlap; `u1 −= dir·(d1·overlap + gap)`, `u2 += dir·(d2·overlap + gap)`, `gap = 1` (0 для стороны сайта). Массы: королева 10000 (её толкают рыцари!), рыцарь 400, лучник 900, гигант 2000.
- Округление Kotlin `roundToInt` = половина вверх; в C# `Math.Round` банковское → использовать `Math.Floor(x + 0.5)`.

## Боссы Wood (тесты рефери `src/test/kotlin`)

- `Level1Player` (Wood 3): две казармы рыцарей и одна лучников на ближайших пустых сайтах, тренирует всё каждый ход.
- `Level2Player` (Wood 2): по одной казарме каждого типа, три башни (докачивает до 400), тренирует всё.
- `config/level4/Boss.java` (Bronze): 140 строк.
- Для Wood достаточно бота «казармы рыцарей + TRAIN каждый ход», в Wood 1 плюс шахты (там нет фиксированного дохода).

## Что переиспользуем из `GasaiYuno1/Code-legends-bot` (LOCM-бот, C#)

Подключать тот репозиторий в сессию и копировать, не переписывать:
- `src/LocmBot/LocmBot.csproj` — настройки, проверенные на CodinGame (`net8.0`, `LangVersion 10`, `InvariantGlobalization`, `TieredPGO`, без пакетов); `nuget.config` (сборка без сети).
- `tools/bundle.py` — склейка в один файл `dist/codingame.cs` (блочные `namespace`, все `using` в начале файла); `tools/paste_page.py` — страница «скопировать код» для телефона.
- `tests/LocmBot.Tests/MiniTest.cs` — мини-раннер тестов без NuGet; `src/LocmBot/TurnClock.cs` — тайминг хода; `Program.cs` — параметры `key=value` для тюнинга.
- `tools/arena/fetch.py` — скрапер реплеев арены через внутренний API (сменить имя пазла на `code-royale`); `fetch.py --handle` — свои бои → винрейт по противникам без CGBenchmark. Нужна сетевая политика с `www.codingame.com`, `*.codingame.com`.
- `tools/arena/spsa.py` + паттерн `BotMatch` — оптимизация весов по винрейту, ~1000 партий/мин при `warmup=0` и 4 процессах.
- Уроки LOCM: прогрев JIT на первом ходу (там 1000 мс); совпадение с сильными ботами и self-play не заменяют арену — каждое изменение проверять на ≥ 200 партиях арены; синтаксически кривой вывод = поражение.

## Предложения Claude к плану (не приняты, обсудить перед этапом 1)

1. Арена self-play на C# поверх своего же симулятора (симулятор = арбитр), а не cg-brutaltester: тысячи партий/мин. Kotlin-рефери — только для сверки; его сборка требует Maven Central, Kotlin 1.2.10 и SDK CodinGame 1.30, в песочнице может не собраться.
2. Сверка симулятора без сборки рефери: (а) реплеи арены; (б) **самопроверка в бою** — бот предсказывает следующее состояние и сравнивает с реальным вводом, расхождения в stderr.
3. Wood-бот не расходный: парсер, вывод, тайминг и склейка — каркас настоящего бота, меняется только политика. Константа лиги в коде.
4. В beam search кроме 8 направлений — макро-действия «идти к сайту X и строить Y». Модель противника: королева статична, казармы продолжают тренировать (прогресс виден), башни стреляют, золото противника по счётчику.
5. Оценка и макро зависят от HP королевы (25 vs 100 — разные игры).
6. Горячее место симулятора — расталкивание (5 подшагов × все пары): отсекать заведомо далёкие пары дешёвой проверкой, порядок обработки сохранять, тогда точность не теряется.
7. Порядок этапов оставить, сборку рефери сдвинуть в конец как необязательную.

## Формат работы (уточнение от Андрея, сессия 1)

Не останавливаться на подтверждения: делать до момента, когда возникает вопрос, или до конца этапа. Перед этапом — короткое объяснение плана, без ожидания «делай».

## Структура репозитория

- `src/RoyaleBot/` — бот. Блочные `namespace Royale { }`, все `using` в начале файла (этого требует склейка). Внешних пакетов нет.
  - `Game/` — `Rules` (лига константой `DefaultLeague` = 4, Bronze+; флаги Mines/Towers/Giants/FixedIncome; `Consts`, `Creeps` — константы рефери), `Site` (сайт + состояние хода + `KnownGold/KnownMaxMineSize`), `UnitInfo`, `TurnInput` (`MyQueen`, `EnemyQueen`, `Income`), `InputParser` (`ReadInit`, `ReadTurn`), `SiteMemory` (помнит золото/размер шахты, когда сайт не виден), `Actions` (`QueenAction` WAIT/MOVE/BUILD с проверкой `Allowed` по лиге, `TurnOutput` с `TrainLine`), `Geom`.
  - `Sim/` — симулятор, построчный порт рефери. `SimState` (`SimSite`, `SimUnit`; игроки по абсолютному индексу: 0 — первый, его королева первая в списке юнитов ввода; `FromInputs(mine, other, turn[, carry])` — состояние из ввода одного или обоих игроков, на ходу 0 `RestoreInitialQueens` повторяет расталкивание королев от углов (200,200)/(1720,800), потому что во вводе их позиции округлены; `ToInputLines(p)` — ввод глазами игрока p с видимостью, неизвестное золото сайта — `?`; `Clone`/`CopyFrom`), `SimStep` (`Step(a0, a1)`: действия игроков с предупреждениями рефери → крипы: 5 подшагов движения + один проход расталкивания, урон, снос шахт, −1 HP, снос королевами → постройки по порядку сайтов → доход Wood → удаление мёртвых → конец → округление `JavaMath.Round`; коллизии через массив `Body` в порядке рефери, без `sqrt` для непересекающихся пар и без пар сайт–сайт — результат бит-в-бит тот же), `SimAction` (разбор ответа как в рефери: лишний токен — убит, неверный тип постройки — убит только при касании), `JavaMath.Round` (Java `Math.round`, а не банковское .NET).
  - `Search/` — этап 3. `Eval` (`EvalWeights` + `Eval.Score(state, me)`: моё HP ×100, HP противника ×30, смерть ±1e6; башни ×3 за HP и шахты ×300 за единицу дохода с убыванием к концу партии; чужой рыцарь −60 за HP остатка **без зависимости от расстояния** (зависимость заставляла королеву вечно убегать в угол от любых рыцарей на карте); свои рыцари +15 за HP × близость к чужой королеве; нет казармы рыцарей −2000, казармы сверх 2 −500; королева под своей башней при угрозе +300; шум ±0.5), `Macro` (`Train`: гигант, если у врага ≥ 2 башен и есть казарма гигантов, иначе все свободные казармы рыцарей по золоту; `Candidates`: WAIT, 8 направлений по 100, постройки на 4 ближайших сайтах: шахта (если золото не 0 и нет чужих рыцарей ближе 250), башня, казарма рыцарей (1, вторая при доходе ≥ 6), казарма гигантов при ≥ 2 чужих башнях, апгрейды своих шахт и башен), `SearchStrategy` (beam search глубиной 12, ширина 16, дедупликация по позиции королевы/постройкам/золоту; противник: королева WAIT, TRAIN по тому же `Macro` с оценкой его золота — старт 100 + доход его шахт − замеченные тренировки; мой TRAIN по `Macro` на каждом шаге; у 16 лучших листьев продолжение «повторяй последнее действие» ещё 10 ходов, итог = 0.3·статика + 0.7·после продолжения; бюджет 28 мс на ход вместе с продолжениями, резерв под них по замеренной цене шага, первый ход 500 мс; `debug=1` печатает кандидатов корня с оценками).
  - `Strategy/` — `IStrategy` (`WarmUp`, `Play`), `RandomStrategy` (ключ `random=<seed>`: случайные постройки всех разрешённых типов и TRAIN всего, для логов сверки), `WoodStrategy` (казарма рыцарей → шахты до `TargetIncome` (полные правила) → 3 башни и их прокачка ниже 500 → ещё шахты; при чужих рыцарях ближе `DangerRadius` — под ближайшую свою башню и качать её, без башни — строить новую на сайте не ближе к рыцарям, в Wood 3 — бежать от рыцарей к дому; TRAIN — все свободные казармы рыцарей по золоту).
  - `Bot.cs` — цикл ходов: стартовый ввод, две строки ответа, любая ошибка → `WAIT`/`TRAIN`, постройка не по лиге → `WAIT`. `TurnClock.cs` — 1000/50 мс с запасом 10. `Program.cs` — стратегия по умолчанию `SearchStrategy`, `wood=1` — `WoodStrategy`, `random=<seed>`; `key=value` для локальных матчей: `league`; Wood: `income`, `towers`, `upgrade`, `danger`, `wreach`; поиск: `depth`, `width`, `ms`, `firstms`, `rollout`, `rolloutw`, `sites`, `giants`, `bar2`, `debug`; веса оценки: `hp`, `ehp`, `tower`, `etower`, `mine`, `emine`, `gold`, `knight`, `eknight`, `reach`, `far`, `giant`, `egiant`, `nobar`, `extrabar`, `maxbar`, `cover`, `noise`.
- `tests/RoyaleBot.Tests/` — тесты без NuGet (`MiniTest.cs`, `[Test]` на `public static void`): парсер, память сайтов, формат ответа, запрет построек не по лиге, мусорный ввод; `SimTests` (округление, `towards`, формула башни, спавн) и `FixturesMatchReferee` (все логи `tests/fixtures/*.log` — 0 расхождений, ≥ 500 ходов); `Replay.cs` — сверка: на каждом ходу состояние из ввода обоих игроков + их ответы → `Step` → ожидаемый ввод следующего хода для обоих сравнивается с логом (`?` — неизвестное симулятору поле, пропускается); `Bench.cs` — шагов/мс.
- `tests/fixtures/*.log` — логи `match.sh -log`: боссы, Wood-бот, случайные боты (лучники, гиганты, хаотичные постройки) по всем лигам. Формат: `# game seed= league= p0= p1=`, `# init player p` + стартовые строки, `# frame 2t+p player p` + ввод хода как есть + `> ответ` (2 строки) + `# summary` предупреждения рефери.
- `tools/bundle.py` → `dist/codingame.cs` (в git, пересобирать перед пушем); `tools/check_bundle.sh` — склейка + сборка её как единственного файла проекта; `tools/paste_page.py` → `build/paste.html` — страница «скопировать код» для телефона (склейка под Bronze и выше, `DefaultLeague = 4`; выбор лиги убран — Wood пройден), публикуется как артефакт.
- `tools/arena/fetch.py --handle <handle> [--max-games N]` — реплеи арены через внутренний API (`build/arena/games/*.json`, сводка `games.tsv`: соперники, счёт, кто победил; свой handle `712b3621df6ce65a3eff2bd78ab18e752899972`, босс Bronze — agent 1870318 «Boss»). В реплее есть stdout **обоих** игроков по кадрам (ответы) и данные просмотрщика; ввода и stderr нет. `tools/arena/replaybot.py <game.json> <index>` — бот-магнитофон: с настоящим рефери и тем же seed воспроизводит партию арены (`match.sh 4 1 <seed> "python3 tools/arena/replaybot.py g.json 0" "... 1" -noswap -log dir`); карта совпадает с ареной точно. `tests -- arenasim <лог> <actions.txt> <hud.txt> [order=old]` — те же действия через симулятор, сравнение золота и HP по ходам с HUD арены (тексты HUD: x=553/1771 HP, 700/1020 золото).
- `tools/extract_turns.py <лог> <игрок> <N>` — ввод игрока за ходы 0..N из лога, чтобы скормить боту с `debug=1` и увидеть кандидатов корня с оценками на конкретном ходу.
- `tools/timeline.py <лог> [шаг] [игрок]` — таймлайн партии по логу `-log`: золото, доход, шахты, башни, HP, крипы, рыцари у королевы, действия. Первый инструмент при разборе проигрыша.
- `tools/referee/` — официальный рефери: `build.sh` (клон `csj/code-royale` в `build/referee/`, правка pom под Kotlin 1.9.24 и `minBy`→`minByOrNull` для JDK 21, сборка Maven, компиляция боссов `config/level1..4/Boss.java`, раннер), `Match.java` (пакет раннера SDK 1.30: партии без просмотрщика через приватные `initialize`/`run`, счёт = HP королевы, −1 = убит; считает предупреждения и «убийства» по summary, `-dump` пишет summary и вывод игроков по кадрам; кадр = полуход, 501 кадр = 250 ходов), `match.sh <лига> <игр> <seed> <A> <B>` (`bot`, `bot:key=value`, `boss`, произвольная команда; стороны чередуются; `-log dir` — полные логи для сверки: ввод каждого игрока перехватывается обёрткой над `getInputStream()` агента, кадр 0 у SDK — инициализация, ответы игроков сдвинуты на кадр). Важно: `--add-opens java.base/java.lang` — без него Guice/cglib SDK падает на JDK 21 и раннер виснет.

## Команды

```
dotnet build src/RoyaleBot -c Release
dotnet run -c Release --project tests/RoyaleBot.Tests [фильтр]
tools/check_bundle.sh                         # dist/codingame.cs + проверка сборки одним файлом
python3 tools/paste_page.py                   # build/paste.html
tools/referee/build.sh                        # один раз: рефери + боссы + раннер (нужна сеть до github и Maven Central)
tools/referee/match.sh 4 10 1 bot boss        # 10 партий против босса Bronze, сиды 1..10
tools/referee/match.sh 2 10 1 bot:towers=2 boss -v -dump build/dump/l2
tools/referee/match.sh 4 3 401 bot:random=7 boss -log build/logs/rnd4          # логи для сверки симулятора
dotnet run -c Release --project tests/RoyaleBot.Tests -- replay build/logs/rnd4 [dump=N]   # сверка симулятора с логами (0 расхождений)
dotnet run -c Release --project tests/RoyaleBot.Tests -- bench [логи] [turn=60] [steps=5000]  # скорость симулятора
dotnet run -c Release --no-build --project tests/RoyaleBot.Tests -- arena games=100 seed=1 ms=12 threads=4 a="" b="wood=1 barlate=1 income=4 towers=3"   # self-play в процессе: 100 партий за ~25 с
python3 tools/arena/tune.py --games 100 --ms 12 --threads 3 [--base "key=value ..."] [--params "mine=3,6 ..."]   # покоординатный подбор весов (журнал build/tune/log.txt)
python3 tools/arena/fetch.py --handle 712b3621df6ce65a3eff2bd78ab18e752899972 --max-games 60                       # реплеи арены -> build/arena
python3 tools/arena/hud.py build/arena/games/<id>.json [шаг]                                                    # HP/золото обоих игроков и их ответы по ходам из HUD реплея
python3 tools/arena/spsa.py --iters 80 --games 300 --ms 6 --threads 4                                           # SPSA по винрейту (θ+ против θ−, 25 весов Eval/поиска, арена в процессе ~160 партий/мин при 6 мс); журнал build/spsa/log.txt, лучший θ build/spsa/best.json, --resume
python3 tools/arena/spsa.py --check build/spsa/best.json --games 2000                                           # θ против умолчаний сборки
python3 tools/arena/spsa.py --vs "eknight=15" "" --games 400                                                    # быстрый матч A против B в арене
python3 tools/arena/prep.py build/arena/games/<id>.json build/arena/prep                                        # actions/hud из реплея для arenasim (+ seed для replaybot)
python3 tools/arena/tooltips.py build/arena/games/<id>.json [от] [до]                                          # HP крипов/башен и радиусы башен по ходам из подсказок реплея
python3 tools/arena/shots.py build/arena/games/<id>.json <лог воспроизведения>                                  # выстрелы башен: правило цели и формула урона против подсказок
python3 tools/arena/poscmp.py build/arena/games/<id>.json <dump arenasim> [от] [до]                             # позиции крипов и королев по ходам против просмотрщика
python3 tools/arena/viewpos.py build/arena/games/<id>.json [лог воспроизведения]                                   # позиции королев/крипов из view, первое расхождение с логом
```

## Окружение песочницы

- `dotnet` не предустановлен; `dot.net` и `builds.dotnet.microsoft.com` закрыты политикой, ставится `apt-get install -y dotnet-sdk-8.0` из репозитория Ubuntu (8.0.130). JDK 21, Maven и Gradle есть. Maven Central доступен. `www.codingame.com` доступен.
- Рефери собирается ~1 мин (зависимости кэшируются в `~/.m2`). Партия против босса ~1 с (старт JVM бота-босса и dotnet), 4 ядра — 4 параллельных `match.sh`.

## Статус

- 2026-09-08, сессия 1. **Этап 1 сделан**: каркас, тесты, склейка 18k символов, рефери и матчи против боссов; по 10 партий на сидах 1..10: Wood 3 **10:0**, Wood 2 **8:2**, Wood 1 **10:0**, Bronze **10:0**, 0 предупреждений и таймаутов. Андрей залил бота, Wood пройден, бот в Bronze.
- Сессия 1, продолжение. **Этап 2 сделан**: симулятор сверен с рефери на 24 партиях всех лиг (боссы, Wood-бот, случайные боты с лучниками и гигантами) — **1919 ходов, 0 расхождений**, включая ход 0 (восстановление дробных позиций королев) и предупреждения рефери (TRAIN без золота, перестройка тренирующейся казармы). 10 логов в фикстурах, тест `FixturesMatchReferee`. Найденные при сверке тонкости: память о золоте истощённой шахты (шахта исчезла при золоте ≤ дохода — истощена, иначе снесена; чужая невидимая шахта копает дальше) — в `SiteMemory`. Скорость (`bench`, копия + шаг, WAIT обоих): 0 крипов 400–700 шагов/мс, 8 крипов 160, 15 крипов 24 (≈ 40 мкс) — узкое место по-прежнему расталкивание n² при скоплении крипов; резерв: сетка/список соседей, без `CopyFrom` на шаг.
- Сессия 1, продолжение. **Этап 3, первая версия поиска** (`Search/`): против Wood-бота **30:0**, против босса Bronze **6:0**, против «черепахи» (промежуточная версия с оценкой башен 3/HP и шахт 300/доход, которая строила только башни и ничего больше: `build/bot-ref` через `tools/snapshot_ref.sh`) 6:3:1, self-play результативный (2:2, не ничьи), максимум 25 мс на ход. Путь к этим весам: (1) оценка с зависимостью штрафа за чужих рыцарей от расстояния гнала королеву в угол карты навсегда — штраф сделан плоским; (2) башни дороже шахт → «черепаха» без экономики; (3) шахты 15 за золото добычи → 130 ходов без казармы и 1500 золота в кармане; (4) веса выведены из HP-эквивалентов: волна рыцарей против защищённой королевы даёт 5–7 HP, значит 1 золото ≈ 4 очка при HP противника 50. Казарма гигантов не видна в горизонте (10 ходов тренировки + путь) — ей явный бонус при ≥ 2 чужих башнях. Таймауты в локальных матчах = перегрузка процессора (не больше 2 матчей параллельно на 4 ядрах). Бот залит в арену (Bronze, ~500-е место было у Wood-эвристики).
- **Арена, Bronze**: поиск-версия дошла до **#1 Bronze из 2293, но босса не пробила** (rank 622). По 60 реплеям: против босса 3:2; два поражения по −1 = **таймауты** (пустой ответ на ~36-м и ~40-м ходах при бюджете 25 мс — серверы CodinGame медленнее песочницы). Босс Bronze (agent 1870318) сильнее `config/level4/Boss.java`: 2 шахты на 0–5 ходах, 3 башни к 18-му, казарма рыцарей на 26-м, дальше TRAIN каждый ход и добор шахт/башен; в одной партии убил нашу королеву (60 HP) к 67-му ходу, пока наш бот с одной шахтой бродил. Разбор через `extract_turns.py` + `debug=1` показал **глубину поиска 1–3**: резерв времени под продолжения листьев (16 × 10 шагов по замеренной цене шага) съедал бюджет, а холодный JIT давал 30+ мкс на шаг. Исправлено: бюджет 18 мс, продолжения только 6 листьев × 8 ходов, резерв ≤ трети бюджета, `[MethodImpl(AggressiveOptimization)]` на `Step`/коллизиях/`Eval.Score`/`Expand` (глубина 12 с первого хода), после `FineDepth`=4 кандидаты только «то же действие» или WAIT (уровень стоит 32 симуляции вместо 200), TRAIN без аллокаций, `GCLatencyMode.SustainedLowLatency`. Башня в оценке: 400 за существование + 0.5 за HP (прокачка на +96/ход больше не перевешивает шахту).
- **Расхождение с ареной**: воспроизведение партии арены локальным рефери (`replaybot.py`) даёт ту же карту и HP, но с 18-го хода HP королевы босса под ударами наших рыцарей на 1 больше в арене, дальше всё расходится (партия в арене на 4 хода длиннее). Старый порядок сущностей в коллизиях (`SimState.InterleavedQueens`, закомментированный вариант `allEntities()` в `Referee.kt`) совпадает дольше (21 ход против 18), но тоже не полностью → в арене другая версия рефери с чуть иной физикой контакта рыцарь–королева. Позиции из `view` разобраны (`tools/arena/viewpos.py`: группа юнита = id спрайта `Unite_*` + 1, владелец по `Unite_Base_Bleu/Rouge` предыдущего спрайта, иконка казармы тоже `Unite_Fantassin` — фильтровать по обновлениям в кадре): королевы совпадают до 11-го хода, рыцари арены с первых ходов после спавна отстают от локальных на 30–80 px (замыкающие сильнее, скорость та же ~95 px/ход, спавн внутри сайта с выталкиванием на 92 px как в коде), т.е. в арене крипы сильнее мешают друг другу. Проверено и отвергнуто: полное расталкивание на подшаг (`SimState.SubstepIterations=999`, хуже), старый порядок сущностей (чуть лучше, не точно). Кандидаты: другой `gap`/массы, другой порядок ходов крипов, другая версия SDK-физики. Инструменты: `tests -- arenasim ... iters=N dump=file` пишет позиции по ходам.
- **Арена self-play в процессе** (`tests -- arena`): симулятор как арбитр, карта как в `MapBuilding.kt`, 100 партий за ~25 с при бюджете 12 мс (4 потока). **Тюнер** (`tools/arena/tune.py`, покоординатно, панель = имитация босса + текущие веса, 100 партий на точку, порог +0.03): базовые веса 0.655 против имитации → принято `mine=3 tower=1 towerneed=1200` (0.91 против имитации, 0.53 против прежних весов); `mine=6` даёт 0.28 (перекос в экономику = смерть), остальные ключи (`towercalm`, `exposure`, `eknight`, `knight`, `ehp`, `gold`, `cover`) в пределах шума. Значения стали умолчаниями. Настоящий рефери, бюджет 18 мс: имитация босса 7:2:1, `Boss.java` 6:0, черепаха 1:2:7, максимум 13 мс на ход, глубина 12 всегда. Версия опубликована для заливки (Bronze, цель — босс).
- Тюнер, раунд 2 (панель с новыми весами, база 0.693): принято только `exposure=1` (0.728); `width=24` 0.750 (не дотянул до порога, на CodinGame при 18 мс и ширине 16 поиск заканчивается за 13 мс — ширину 24 можно пробовать), `width=8` 0.585 (плохо), `minefar=0.2` 0.645, `giantbar=3000` 0.657, `etower=0.25` 0.645 — хуже; остальные ключи в шуме (±0.03 при 100 партиях). Веса на плато для этого соперника; дальше рост даст другой соперник в панели (сильный self) или структурные изменения поиска.
- **Арена, вторая заливка** (agent 6719724; каждая заливка = новый agentId, `fetch.py --agent`): 23:7 по 30 партиям, ранг ~385, но играет «черепахой»: казарма, 2–3 башни, 1–6 сайтов шахт, MOVE в 50–70% ходов (по словам Андрея «мувается как пьянь»). Причина 1: тюнер переобучился под имитацию босса — черепаха её бьёт, потому что рыцари имитации гибнут о башни; веса `mine=3 tower=1 towerneed=1200` откачены (`mine=4 tower=0.25 towerneed=800`). Причина 2, структурная: **луч со статической оценкой близорук** — прокачка башни даёт +48/ход сразу, а поход к сайту за казармой/шахтой первые 3–5 ходов не даёт ничего, и такие ветки вылетают из 16 лучших уже на второй глубине; при 250 золота бот 30 ходов не строил казарму. Лечение: **потенциальная оценка** (`Eval`: половина штрафа `NoBarracks` и штрафа `EconShort × (EconTarget − доход)` снимается по мере подхода королевы к нужному сайту, `ShapingDist` 1200) + прокачка башни дешевле (`tower=0.25`), + **фазы в `Macro.Candidates`**: башни предлагаются только при живых чужих рыцарях, или при чужой казарме рыцарей до `TowersEarly`=2, или при доходе ≥ `TargetIncome`=6 до `MaxTowersCalm`=3; прокачка только ниже `UpgradeCalmBelow`=350 или при рыцарях; всегда добавляются `FarMineSites`=2 ближайших свободных сайта с золотом на своей половине (цель для похода). Результат локально: без угрозы доход 23 к 75-му ходу, под ранним рашем 2–3 башни и оборона, MOVE почти исчезли; слабое место — при раннем раше башни занимают ближние сайты и доход застревает на 4.
- **Таймауты в локальных матчах** разобраны: в убитых ходах сам поиск занимал 1–16 мс, а 23-мс ход случился у Wood-бота без поиска. Замер фаз в `Bot.Run` (`read` — от первой строки ввода до конца разбора, `think`, `gap`): `think` ≤ 18 всегда, но `read` доходит до 12 мс — ввод от рефери приходит с задержкой (пишет поток `AsynchronousWriter` JVM; паузы GC рефери до 9 мс, `JAVA_OPTS=-Xlog:gc:file=...`), и это время идёт в таймер рефери. Это шум локальной машины (4 ядра, JVM с кадрами просмотрщика в памяти). Лечение для статистики: локальный рефери принимает `-Dturn.max.time` (патч `Referee.kt` в `build.sh`), `match.sh` берёт его из `TURN_MS` (по умолчанию 50, для статистики 200), а честность бота контролируется по `think` в stderr. На CodinGame бюджет 18 мс оставляет 32 мс запаса.
- **Кто играет в Bronze** (100 реплеев обоих наших агентов, классификация по stdout соперника): 75% «экономические» (≥ 4 сайта шахт, 4–11 башен, рыцари, казарма в среднем на 10-м ходу, первая башня на 13-м), 16% смешанные, 5% рашеры, 4% чистые черепахи; гиганты у 25%, лучники у 6%. Наш счёт по типам (версия-черепаха): экономические 53:22, смешанные 11:5, рашеры 2:3, черепахи 4:0. Босс — экономический тип. Вывод: локальная черепаха-эталон не главный ориентир; панель тюнера: имитация босса + экономический (`wood=1 income=8 towers=4 upgrade=600`) + черепаха + рашер + self.
- Против черепахи-эталона версия с экономикой проигрывает 2:8 не в дебюте (ранняя оборона: 1–2 башни к 10-му ходу, HP 76–80 к 25-му), а одной волной на 40–50 ходах, когда королева добывает в 500 px от своих башен (`exposure=1` мал против ценности шахт); их HP не пробить (5–9 башен). Гиганты только при казарме рыцарей (`Eval.GiantBarracks`), готовность обороны `Readiness × (DefenseNeed − HP башен в DefenseRadius от королевы)` при угрозе, премия `Persist` за продолжение прошлого действия.
- **250 ходов, а не 200** (40 реплеев агента 6719724, версия-черепаха: 28:12, все партии по 501 кадру = 250 ходов; в исходниках рефери на GitHub `maxTurns = 200`). Симулятор объявлял конец партии на 200-м ходу, оценка обнуляла башни/шахты к 160-му, а последние 50 ходов (20% партии) поиск был слеп — в реплеях бот в это время бродит MOVE. Исправлено: `Consts.MaxTurns = 250`, локальный рефери `-Dturn.max` (`TURN_MAX` в `match.sh`, 250 по умолчанию). Там же: **33 из 40 партий доходят до лимита и решаются разницей HP**, три поражения — с разницей в 1 HP (99:100, 79:80, 22:23), выигрыши часто +3…+10. Добавлен член оценки `Lead × tanh((myHp − enHp)/LeadScale)`, нарастающий за `LeadTurns`=60 ходов до конца (`lead`=5000, `leadscale`=5, `leadturns`=60; ключи для опытов), и терминальная оценка исхода по лимиту (`GameOver` с `Winner` = ±0.1·Dead). Инструмент `tools/arena/hud.py <game.json> [шаг]` — HP/золото обоих игроков и их ответы по ходам из HUD просмотрщика. В тех же реплеях старая версия 200+ ходов сидит на 20 золота без единой шахты (то, что чинит потенциальная оценка); арена ещё играет старой версией — новую нужно залить.
- **Баг TRAIN** (найден по предупреждениям рефери «Barracks 5 is training», до 10 за партию): `Macro.Train` отдаёт общий буфер по числу казарм, а ответ хода брался из него **до** поиска — поиск перезаписывал буфер состояниями будущих ходов, и в ответ уходил TRAIN занятой казарме или без золота (предупреждение = потерянный ход тренировки). Исправлено: TRAIN по корню считается после поиска и клонируется. Воспроизведение: `extract_turns.py <лог> 0 75` + бот на stdin.
- **Волна в никуда**: против черепахи бот на 0-м ходу ставил казарму и на 4-м отправлял 4 рыцарей в 1–2 башни — 80 золота и старт экономики впустую, дальше партия 94:95 по HP. TRAIN не был решением поиска (Macro тренирует при 80 золота всегда), поэтому добавлена проверка волны в корне (`SearchStrategy.FilterWave`, ключи `wave`=1 — минимальный урон чужой королеве, `waveturns`=40): симуляция тренировки и 40 ходов с неподвижными королевами без новых тренировок; если чужая королева теряет меньше `wave` HP, рыцари из TRAIN убираются (гиганты остаются). Гиганты: не тренируем и не копим на них за `giantleft`=40 ходов до конца (исход решает HP, не башни) и копим только если 140 достижимы за `giantsave`=15 ходов при текущем доходе (иначе при иссякших шахтах бот сидел на 120 золота до конца партии).
- **Тюнер 3b** (панель имитация босса + экономический `wood=1 income=8 towers=4 upgrade=600` + self, 100 партий на точку, порог 0.025, 31 мин): база 0.67/0.98/0.44 → принято `exposure=4` (0.81/0.98/0.505) и `readiness=3` (0.85/1.00/0.545), итог 0.697 → 0.800. Отвергнуто как шум или хуже: `defneed=1000`, `econshort=800`, `econtarget=8`, `minefar=0.7`, `width=24`, `towerneed=1200`, `mine=3`, `tower=0.5`, `persist=400`; `mine=6` снова провал (0.56 против имитации босса). Раунд 3 с эвристическими черепахой и рашером в панели (побеждались на 100%) остановлен как неинформативный.
- **Арена, третья заливка** (agent 6721245, версия 3cb3c0a/672e41b): **46:13:1 по 60 партиям, ранг 634, Bronze #13 из 2293**; характер игры сменился — 39 побед убийством королевы (старая версия побеждала по HP на лимите). Проигрыши: 9 убийств нас, 4 по HP. Быстрые (королева убита на 40–75 ходу при старте с 25–55 HP; cclaude42, EJtheMaster, Razupaltuff, auchan): противник с 3 шахтами и одной казармой шлёт волну каждые 5 ходов, наша королева между волнами уходит копать шахты и попадает под удар в открытом поле; при 25–30 HP хватает двух волн. Причины в коде: (1) **доход чужой шахты не виден (−1) и попадал в симулятор как `Rate = −1`** — противник в поиске терял золото и никогда не тренировал, поиск не видел потока рыцарей (ключ `emrate`: предполагаемый доход невидимой чужой шахты = максимум сайта, если видели, иначе 2; −1 = старое поведение); (2) **кандидаты построек брались с 4 ближайших сайтов любого владельца** — на чужие строить нельзя (предупреждение рефери), а когда все 4 наши, строить было нечего, и бот бродил (в 902225596 при 142 золота и 3 чужих башнях не поставил казарму гигантов); теперь `NearSites`=4 ближайших свободных + `NearOwn`=3 своих (прокачка, шахта → башня при угрозе); (3) партия с Vinland (42:45): 13 HP потеряны, пока королева копала шахты в радиусе его прокачанной башни — штраф `erange`=300 за лист с королевой в радиусе чужой башни. Медленные проигрыши (Shadaia 3:8, cclaude42 25:33) — гонки рыцарей, проиграны на 5–8 HP из-за одной пропущенной ранней волны (−34 HP на 36–48 ходах без единой башни при чужой казарме с 12-го хода).
- **Глубина поиска против числа кандидатов**: `bench` — 452 шагов/мс без крипов и **81 с 8 крипами**; новые кандидаты давали 23 на ход против 16 у залитой версии, и на 13% ходов (против 4%) глубина падала до 1–3 — отсюда проигрыши эталону. Урезано: `NearSites`=3 свободных, `NearOwn`=2 своих, казармы только на `BarracksSites`=2 ближайших свободных, шахта → башня только при живых рыцарях (18 кандидатов на ход). Смотреть гистограмму `depth` в stderr при каждом изменении кандидатов. Проверка на одинаковых входах (`extract_turns.py` + обе сборки на stdin): на поздних ходах с толпой крипов **обе** сборки дают ~50 узлов/мс и глубину 12 лишь на трети ходов (120–160 узлов/мс и глубина 12 всегда без крипов) — новые признаки скорости не стоят (`emrate`/`expeta`/`erange` по отдельности ничего не меняют), разница в матчах — от позиций. Следующий резерв: ускорить `Step` при многих крипах (коллизии).
- Проверка правок настоящим рефери: против экономического и имитации босса по-прежнему 6:0; против залитой версии (`build/bot-ref-ref3`) 9:11 по 20 партиям, после урезания кандидатов 14:16 по 30 (6:6, 5:7, 3:3) — паритет в пределах шума; self-play не видит разницы, потому что залитая версия не играет как арена (не шлёт поток рыцарей); решает арена. `emrate=-1` против `emrate=2` в self-play 51:48 (шум). Exposure с учётом времени подхода рыцарей (`expeta`=0.5: штраф только за ту часть пути до укрытия, которую королева не успевает пройти, пока ближайший рыцарь идёт к ней; раньше — за всё расстояние, и королева замирала у башен при рыцарях на другом конце карты): self-play 0.8 против 0 — 58:40:2, 0.5 против 0.8 — 55:44:1; принято 0.5 (рыцари быстрее королевы, запас нужен). Правило «чужая королева ≤ 20 HP → без накопления на гигантов и без фильтра волны» (`lowhp`) спасло партию с противником на 12 HP за 3 башнями, но против эталона дало 3:7 вместо 6:4 на тех же сидах (на картах с 25–40 HP срабатывает всю партию) — выключено (`lowhp=0`).
- **Silver** (agent 6721682, версия b494934; босс Bronze пробит): **38:21 по 59 партиям, ранг 483, Silver #52 из 192**. Проигрыши: 9 убийств (три на 40–50 ходу при старте обоих с 25 HP: противник ставит 2–3 шахты, казарму на 8–16 ходу и шлёт волны, две волны = 25 HP), 11 по HP на лимите, **1 таймаут** (пустой ответ на 99-м ходу в большой драке — 36–62 юнита в кадре; бюджет поиска снижен 18 → 15 мс, `MaxMs`). По стартовому HP: >70 — 20:8, 41–70 — 12:7, ≤40 — 6:6 (`hud.py`, HP из текста HUD x=553). Добавлен квадратичный штраф за низкий HP королевы `LowHp × (LowHpLevel − HP)²` (`lowhpw`=5, `lowhplevel`=40): при 25 HP одна волна — половина жизни, линейная цена HP этого не видела; self-play 57:42 против версии без штрафа — принято; против залитой версии на картах с низким HP 5:5. Проигрыши по HP в Silver — одна пропущенная волна на 30–50 ходах (−20…−30 HP), потом 150 ходов пата за башнями; cg123 (3 поражения) водит королеву по нашей половине, уходя от рыцарей. К 50-му ходу в победах теряем в среднем 11 HP (противник 15), в поражениях 16 (противник 11) — ранний размен решает партию. `safehp=300` (укрытие только от 300 HP башни) — 50:50, оставлено 100. Панель: имитация босса 6:0, экономический 6:0.
- **Физика арены восстановлена по реплеям** (арена крутит внутреннюю версию рефери, которой нет на GitHub; инструменты: `tools/arena/prep.py` — actions/hud из реплея, `replaybot.py` + `match.sh -noswap -log` — та же карта локальным рефери, `tests -- arenasim` с ключами `order=old|new spawn=github target=N damage=github dmin= dradius=0 khp= iters= dump= verbose=1`, `tools/arena/poscmp.py` — позиции крипов и королев по ходам против просмотрщика, `tools/arena/tooltips.py` — HP крипов/башен и радиусы башен по ходам из подсказок реплея, `tools/arena/shots.py` — все выстрелы башен с геометрией и проверка правил цели/урона). Найдено и сделано умолчанием: (1) **рыцарь 25 HP**, не 30 (17 528 спавнов в подсказках; гигант 200, лучник 45 как в коде); (2) **спавн**: точка `towards(чужая королева, 30)` от центра сайта плюс углы единичного квадрата (+1,−1), (−1,+1), (+1,+1), (−1,−1) у обоих игроков (ключевые кадры t=0 просмотрщика; в GitHub — (±i,±i) до towards), из этих точек расталкивание даёт позиции арены точно (`SimState.ArenaSpawn`); (3) **порядок тел в коллизиях** как до PR #3 (крипы игрока, его королева, крипы второго, его королева; `InterleavedQueens = true`); (4) **урон башни**: 3 + (радиус − расстояние до центра сайта)/200 **без вычитания радиуса сайта** (46 из 51 выстрелов, остальные — добивания; GitHub вычитает; `TowerDamageMode = 3`); цель — ближайший к башне крип, как в GitHub (44 из 51, остальные семь — равноудалённые в 97,x px, где целые координаты просмотрщика не различают); HP/радиусы башен совпадают точно; (5) 250 ходов. Совпадение по HUD (золото и HP обоих) по четырём партиям: 51, 112, 22, 25 ходов (было 18–21); остаток — расхождения в десятые доли пикселя при расталкивании в толпе (первое в партии 2 — на ходу спавна рядом со своей королевой, 1 px), которые меняют лишь, кто из равноудалённых рыцарей получит выстрел; проверенные и отвергнутые гипотезы: королева перед крипами в списке, крипы до действий королев, цель рыцаря по позиции королевы на начало хода, цель башни «ближайший к своей королеве»/«мин. HP»/«не бить уже обстрелянного», таяние башни до выстрела, 2+ прохода расталкивания на подшаг, старые формулы урона 2018-04-05/06. Локальный рефери пропатчен так же (`build.sh`), фикстуры перегенерированы.
- **Тюнер 4 под физику арены** (та же панель, 100 партий на точку, 51 мин): база 0.89/1.00/0.48 → принято `eknight=15` (0.91/1.00/0.555, итог 0.790 → 0.822); `knight=3` (0.832) и `econtarget=8` (0.840) выше, но в пределах порога 0.025 — не приняты, кандидаты на следующий раунд; хуже: `exposure=6`, `defneed=900`, `towerneed=1200`, `eknight=30`; шум: `tower=0.5/0.15`, `readiness=5`, `mine=3`, `lowhpw=10`, `wave=2`. Проверка рефери с `eknight=15`: экономический 6:0, имитация босса 6:0, черепаха на картах с 30 HP 6:0 (было 2:4).
- **Тюнинг весов по объёму, а не руками** (Андрей): покоординатный тюнер по 100 партий на точку принимает шум (±5%) и отбрасывает настоящие сдвиги. Написан `tools/arena/spsa.py` (порт из LoCM): 25 весов оценки и поиска, итерация = 300 партий θ+ против θ−, шаг ∝ (2·winrate − 1), проверка против умолчаний каждые 10 итераций по 600 партий, `best.json`/`state.json`/`--resume`. Скорость арены: 58 партий/мин при 12 мс, 158 при 6 мс, 277 при 4 мс, 559 при 3 мс; выбрано 6 мс (близко к эффективной глубине на сервере CodinGame, где 15 мс на более медленном CPU); проверка стороны при одинаковых весах 94:103:3. Первый прогон: 80 итераций ≈ 3 ч.
- **Ускорение шага в толпе** (профиль `bench profile=1`, мкс на шаг при 13–15 крипах под нагрузкой: фаза крипов 26 = расталкивание 14 [построение соседей 5, пять проходов 7, загрузка/сохранение тел 1] + хвост 8 (снос шахт через `ClosestSite` по 24 сайтам на каждого крипа) + движение 2; копирование состояния 0,08 — не хотспот). Сделано без изменения результата (фикстуры бит-в-бит): **списки соседей** для расталкивания (пары ближе суммы радиусов + 320 px, строятся раз за ход на подшаге 0 и при спавне; порядок обхода пар сохранён), тела сайтов грузятся один раз на экземпляр, строки сайтов зеркалятся из строк юнитов, снос шахт ищет ближайший сайт по списку соседей. Итог: шаг в толпе ~25 → ~17 мкс (59 шагов/мс против 40–47), поиск на поздних ходах партии 11000: 67 узлов/мс против 49, глубина 12 на 19 из 48 ходов против 17. Остаток: пять проходов расталкивания ~7 мкс (пол), хвост 4.
- Следующее: (а) результат в арене; (б) третий раунд тюнера с **разнообразной панелью** (имитация босса + черепаха + рашер + self), иначе переобучение (`safe`, `minefar`, `giantbar`, `towerbase`, `rollout`, `width`) и панелью с новыми весами; (в) модель противника с движущейся королевой; (г) точность против арены (см. выше).
